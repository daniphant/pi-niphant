# Changelog

## 0.1.0

- initial release
- replaces Pi's default working text with rotating whimsical messages
- adds elapsed runtime and `esc to interrupt`
- adds animated shimmer styling inspired by Codex CLI
- uses Pi theme tokens for its shimmer so it inherits the active Pi theme
